import * as fromClearStateAction from './clear-state.actions';

describe('Clear State Action', () => {
  it('should create an instance of Clear State action', () => {
    expect(fromClearStateAction.ClearState).toBeTruthy();
  });
});
